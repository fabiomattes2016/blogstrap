<% if user_signed_in? %>
  <div class="dropdown">
    <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
      <%= current_user.email %>
    </button>
    <ul class="dropdown-menu">
      <li>
        <%= link_to "Editar usuário", edit_user_registration_path, class: "dropdown-item" %>
      </li>
      <li>
        <hr class="dropdow-divider">
      </li>
      <li>
        <%= link_to "Sair", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </li>
    </ul>
  </div>
<% else %>
  <%= link_to "Cadastro", new_user_registration_path, class: "btn btn-sm btn-secondary", style: "margin-right: 2px;" %>
  <%= link_to "Login", new_user_session_path, class: "btn btn-sm btn-outline-secondary" %>
<% end %>
